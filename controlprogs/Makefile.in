include ../Makefile.general

VPATH=@srcdir@
top_srcdir=@top_srcdir@

all: setlink 

setlink: setlink.o 
	$(CC) $(LDFLAGS) -o setlink setlink.o $(LIBS) 

clean:
	rm -f *.o
	rm -f setlink

distclean: clean
	rm -f *~
	rm -f core*
	rm -f Makefile 
